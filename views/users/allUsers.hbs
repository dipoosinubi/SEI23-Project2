<h1>All Users</h1>

<a href="/users/new">New User</a>

<div> 
    {{#each users}}
    <form>
      <a href="/users/{{_id}}">{{name}}</a>
      {{username}}
      {{location}}
      {{age}}
    </form>
    {{/each}}
</div>